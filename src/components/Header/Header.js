import './Header.scss';

function Header(){
    return(
        <header>
            <div>BGCR</div>
            <button>
                로그인
            </button>
        </header>
    );
};

export default Header;