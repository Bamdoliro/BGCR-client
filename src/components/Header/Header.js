import './Header.scss';

function Header(){
    return(
        <header>
            <div>BGCR</div>
            <input type="button" value='로그인' />
        </header>
    );
};

export default Header;