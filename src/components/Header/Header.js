import './Header.scss';

function Header(){
    return(
        <header>
            <div id="logo">BGCR</div>
            <button id="login-button">
                로그인
            </button>
        </header>
    );
};

export default Header;